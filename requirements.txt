PyQt6>=6.6,<7.0
PyAudio>=0.2.14
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26
scipy>=1.12
onnxruntime>=1.17
pydantic>=2.5
loguru>=0.7
httpx>=0.26
aiohttp>=3.9
protobuf>=5.0,<7
keyring>=24.3
pynput>=1.7
PyYAML>=6.0
soundcard>=0.4.3
python-dotenv>=1.0
websockets>=12.0
edge-tts>=6.1
# Windows-only extras (import-guarded on Linux)
comtypes>=1.4.0; platform_system=="Windows"
pycaw>=20240210; platform_system=="Windows"

# Economy mode cloud ASR (Fun-ASR / Paraformer via DashScope)
dashscope>=1.20
# Economy local ASR (sherpa-onnx streaming Zipformer / SenseVoice offline)
sherpa-onnx>=1.10
# Economy local ASR (faster-whisper, CTranslate2 加速 Whisper)
faster-whisper>=1.0
# Economy ASR: Windows Live Captions (UI Automation, 仅 Win11 22H2+)
uiautomation>=2.0; platform_system=="Windows"
# Economy local MT (NLLB via CTranslate2)
ctranslate2>=4.0
transformers>=4.40
huggingface_hub>=0.23
sentencepiece>=0.2
PySocks>=1.7
socksio>=1.0
# Economy local TTS (Kokoro ONNX); edge-tts kept for typed translate + fallback
kokoro-onnx>=0.4
# Resource monitoring (CPU/RAM) and NVIDIA GPU monitoring
psutil>=5.9
pynvml>=11.0
# Optional Chinese G2P for Kokoro v1.1-zh: pip install 'misaki[zh]'
# Optional local MT fallback (heavy): pip install argostranslate
# Optional offline TTS fallback: pip install pyttsx3
